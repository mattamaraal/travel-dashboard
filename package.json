{
  "name": "travel-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "logs": "clasp logs",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0"
  }
}